# CLAUDE.md 에 허용되는 H2 섹션 목록 (정확히 일치해야 한다).
#
# 왜 별도 파일인가: 섹션 스프롤이 CLAUDE.md 비대화의 1차 경로다. 새 섹션을 만들려면
# 문서와 이 파일을 **둘 다** 고쳐야 하므로, "일단 섹션 하나 추가"가 안 된다.
# 섹션을 늘리기 전에 그 내용이 docs/ 에 가야 하는 게 아닌지 먼저 따진다.
#
# 형식: 헤딩 줄 전체를 그대로 한 줄에. '#' 로 시작하는 줄과 빈 줄은 주석으로 무시된다.
# 제목을 바꾸면 이 파일도 같이 바꾼다 (가드가 "사라진 섹션"으로 경고한다).

## 세션 시작 시 — 읽는 양을 고정한다 (이력이 길어져도 늘지 않는다)
## 작업 규칙 (항상 적용)
## 컨텍스트 예산
## 위치 인덱스
## 커밋 규칙
